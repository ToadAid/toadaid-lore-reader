{
  "name": "toadaid-lore-reader",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "build": "ASTRO_TELEMETRY_DISABLED=1 astro build",
    "import:canonical": "node scripts/import-canonical-lore.mjs"
  },
  "devDependencies": {
    "astro": "^5.16.0",
    "typescript": "^5.9.0"
  }
}
